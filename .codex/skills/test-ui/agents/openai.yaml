interface:
  display_name: "Test UI"
  short_description: "Run scripted CLI UI tests"
  default_prompt: "Use $test-ui to run the CLI test cases in test/ui-test-plan.md and report the console input and output."
